services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: microlearn-app
    env_file:
      - .env
    ports:
      - "${APP_PORT:-8000}:8000"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
